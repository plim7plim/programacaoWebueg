// Comentário em JavaScript: // para uma linha, /* ... */ para várias linhas

// function = declara uma FUNÇÃO (um bloco de código com nome, que só roda quando é chamado)
// saudacao = nome da função | () = parâmetros (aqui não tem nenhum) | { } = corpo da função
// Ela é chamada pelo HTML no botão: onclick="saudacao()"
function saudacao(){
    alert("Você clicou nesse botão!"); // alert() = mostra uma caixinha de aviso na tela com o texto. ; = fim da instrução
}
